---
layout: ../../layouts/DocsLayout.astro
title: "Astro Installation Guide"
---

# Astro Installation Guide

To start a new project, run the following command in your terminal:

```bash

    # create a new project with npm
    npm create astro@latest


```
Follow the prompts to configure your project. Once created:

1. `cd` into your new project directory.

2. Run `npm install`.

3. Start the dev server with `npm run dev`.